# ServiceAccount for the Token Broker.
#
# The Token Broker is a stateless, in-memory service. It does NOT call the
# Kubernetes API, so it needs no Role/RoleBinding — just an identity to run as.
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: token-broker
  namespace: kagenti-system
  labels:
    app: token-broker
