apiVersion: v1
kind: Service
metadata:
  name: bundle-service
  namespace: system
spec:
  type: ClusterIP
  selector:
    app: bundle-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
